# Java API Docs Generator

## Purpose
This skill adds interactive API documentation to an existing Java Spring Boot service with minimal configuration and minimal code churn.

## What it does
- Adds the SpringDoc OpenAPI dependency
- Updates application properties for Swagger UI and API docs endpoints
- Creates an OpenAPI configuration class when needed
- Detects existing project structure and works with Maven or Gradle
- Leaves the existing controller code largely untouched
